# PanguPay Web

PanguPay Web 是 Pangu 快速转账系统的新一代用户界面。它首先是一款可信、易用的钱包，同时也是 TXCer 与 GQNC 研究成果的可视化入口。

## 产品承诺

- 用户先获得可支付的 TXCer，后台再完成本地 GQNC 认证。
- 快速可用、后台审计和本地认证必须被清楚区分，不用一个含糊的“成功”状态代替。
- 金额、交易 ID 和安全证据必须精确，不为视觉效果牺牲协议真实性。
- 首页负责建立品牌感，钱包负责高频操作。两者共享一种设计语言，但信息密度不同。

## 当前阶段：Phase 3

Phase 0 的品牌基座继续保留，Phase 1 已把静态切片升级为正式钱包基座，Phase 2
完成组织接入及真实交易。当前 Phase 3 在 **Ledger Plane 流动账本** 上补齐账户管理与只读链上观测：

- 无导航沉浸式首页。
- 创建、加密备份、独立恢复材料、导入、解锁、忘记密码恢复和自动锁定。
- 与 Go `wallet-keystore/wallet.json` v1 互通的浏览器加密钱包。
- 响应式“流动账本”正式导航与钱包总览。
- 单一余额价值场，以及共享连续平面的资产、活动和安全摘要。
- 桌面端总览、发送、活动、安全、担保组织和区块链导航，侧栏底部设置入口，以及移动端“总览 / 发送 / 活动 / 我的”导航。
- 默认折叠协议技术字段的凭证与安全页面。
- Gateway 真实账户、组织、TXCer 生命周期与签发证据同步。
- 收款地址选择、二维码和复制。
- 独立账户普通转账，以及组织成员的普通、快速、混合与跨链转账。
- 普通、快速与跨链转账的真实审核、签名、提交和三阶段结果反馈；发送表单不再展示冗余协议流程图。
- 活动记录的完整 TXID、时间、方向和本地已知阶段。
- TXCer 凭证链、CFAA 后台审计与 ExposureShares 责任份额可视化。
- 多地址账本、真实地址登记、新建地址和 fail-closed 归档。
- 担保组织权威详情、节点构成、加入条件、能力边界和二次确认退出。
- 只读 GQNC 认证链浏览器：认证高度、区块、交易、QC 签名者与委员会状态。
- 本地用户名和头像资料，不进入 keystore 明文秘密或后端共识状态。
- Argon2 解锁在 Web Worker 中执行，解锁动效立即开始而不阻塞页面主线程。
- Obsidian / Pearl 双主题、中文 / 英文和减少动态效果支持。
- protocol-v2 客户端核心与 Go golden vectors。

首页继续承担空间化品牌叙事，钱包则保持安静、精确且适合高频操作。两者只通过
Logo、蓝色价值场和一次 620ms 的入口过渡保持连续；钱包内部不使用常驻装饰动画。

浏览器中的私钥和 RootSeed 只存在于解锁后的内存；IndexedDB 仅保存加密 envelope
和公共账户快照，LocalStorage 只保存主题与语言偏好。

TXCer 安全状态遵循 fail-closed 语义：权威 signer 上下文不足时显示
`Pending/Unavailable`，证据验证失败时显示异常，不把缺失信息伪造成 `Verified`。

## 非目标

- 不复制旧前端的视觉结构。
- 不提前建立大型组件库或通用页面生成器。
- 不引入新的共识、钱包或后端接口。
- 不使用假价格、假吞吐或没有实验依据的实时指标。
- 不提供 GQNC 投票、治理或节点控制能力；区块链页只读取 Gateway 已公开的认证链状态。
- 不为每个数值增加动画；金额、ID、责任份额和安全证据以稳定阅读为优先。

## 后续阶段

后续阶段重点是 Claim、安全退出、治理与研究观测页面；它们必须复用现有 protocol-v2、
Ledger Plane 与后端真实契约，不新增一套平行状态语义。
