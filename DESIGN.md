# PanguPay 设计语言

## 核心气质

设计由两种空间组成：

- **黑曜保险库**：用于首页与品牌叙事。深、静、具有空间感，但不依赖常见的 Web3 紫色光晕。
- **流动账本**：用于钱包主体。安静、精确、高密度、长期耐看。

高级感来自比例、排版、材质和运动。页面不靠卡片堆叠、彩虹渐变或夸张毛玻璃制造“科技感”。

## 色彩

### Obsidian

| Token      | Value     |
| ---------- | --------- |
| Background | `#08080A` |
| Surface    | `#101013` |
| Text       | `#F5F5F7` |
| Muted text | `#A1A1A8` |
| Accent     | `#2997FF` |
| Action     | `#0071E3` |

### Pearl

| Token      | Value     |
| ---------- | --------- |
| Background | `#F2F3F5` |
| Surface    | `#FFFFFF` |
| Text       | `#1D1D1F` |
| Muted text | `#68686F` |
| Accent     | `#0066CC` |
| Action     | `#0066CC` |

品牌展示可以使用更明亮的强调蓝；承载白色小字号文字的主操作使用更深的交互蓝，确保深浅主题均满足 WCAG AA 对比度。

绿色、橙色和红色只表示状态，不参与品牌装饰。

## 排版与布局

- 使用系统字体栈，不下载 Web Font。
- 标题紧凑、正文舒展、数字使用 tabular figures。
- 页面使用大面积留白、少量低噪声边界和明确的纵向节奏。
- 首页强度为 7/10，钱包主体为 4/10，组件页为 3/10。
- 390、768、1024 和 1440 像素是基准宽度，不把移动端视为缩小后的桌面端。

## 形状与图标

- 旧前端主页 Logo 的四环结轮廓是唯一品牌标志，以透明遮罩和 `currentColor` 呈现。
- 通用图标只使用 Phosphor Icons。
- 控件圆角克制；小控件 10 至 14 像素，主要容器 18 至 24 像素。
- 所有触摸目标至少为 44 × 44 像素。

## 运动

| Interaction           | Duration  |
| --------------------- | --------- |
| Press                 | 120ms     |
| State transition      | 160–220ms |
| Page content entrance | ≤300ms    |
| Hero orbit            | 24–32s    |

- 位移短、反馈直接、缓动具有轻微物理感。
- 首页轨道层之间轻微错速，指针视差不超过 12 像素。
- 禁止滚动劫持、长距离飞入、高频闪烁和装饰性加载动画。
- `prefers-reduced-motion` 下停止循环与视差，但保留完整信息。

### 钱包入口与余额反馈

- 首页进入创建或解锁页时，右侧价值折叠场作为共享元素移动并缩放到访问页左侧；移动端改由品牌 Logo 延续空间关系。
- 完整入口过渡约 620ms；不支持 View Transition 时使用 180ms 淡化。
- 解锁页关闭折叠场自身的重复入场动画，避免共享移动完成后再次闪现。
- 钱包内部路由只使用 160–220ms 的短距离切换。
- 余额光带不是常驻装饰，只在首次真实快照、手动刷新或余额实际变化后运行一次。
- 减少动态效果下，入口仅交叉淡化，余额变化只更新文字和状态。

## 流动账本信息层级

- 总资产是首页唯一视觉中心，UTXO 可用与 TXCer 可支付作为组成信息。
- 发送和收款保持同尺寸、同基线；发送是克制蓝色材质，收款是中性材质。
- 总览只展示“可立即支付 / 安全凭证 / 后台审计”用户摘要。
- FastEvidence、AssignAck、LiabilityReceipt、CFAA、ExposureShares 等术语只进入“凭证与安全”页。
- 正常状态保持安静；只有隔离、验证失败或离线快照才提升视觉优先级。

### Ledger Plane 页面合同

- 桌面侧栏固定约 216px，正文最大宽度约 1240px。
- 主导航只保留总览、发送、活动和安全；设置固定在侧栏底部，主题与语言只在设置页管理。
- 桌面账户菜单只保留账户信息、担保组织和锁定；移动端“我的”额外提供设置入口。
- 钱包页面不默认包裹通用卡片。标题、留白和 hairline 负责分组，实体 surface 只用于表单或异常。
- 总览采用“余额价值场 → 资金状态摘要 → 资产 7 列 / 活动 5 列”的单一纵向平面。
- 手机端保留 UTXO 与 TXCer 组成，底部导航为总览、发送、活动和我的。
- 钱包内部路由位移不超过 6px；同步、复制、余额变化以一次性反馈代替循环动画。

## 可用性

- 输入始终有常驻标签、帮助文字和明确错误状态。
- 密码错误只在输入框下方显示一行低饱和反馈，不与大面积异常卡片重复；反馈预留空间，不推动按钮布局。
- 钱包文件确实损坏时才使用独立异常说明，并直接引导恢复；不得暴露英文内部错误或使用阻断式系统弹窗。
- 解锁页在常见桌面高度内固定为一屏；创建内容较长时只允许表单平面内部滚动。恢复页在极矮视口保留滚动能力，但隐藏内部滚动轨道。
- 创建、加密备份导入与私钥恢复共享同一个顶部标题锚点；切换模式不得改变标题高度，较长表单只向下延伸并在隐藏轨道的表单平面内滚动。
- 键盘焦点清晰，不能只依赖颜色表达状态。
- 输入焦点使用控件内部的细描边，不使用可能被固定视口或滚动容器裁切的外扩光环。
- 恢复与清除是互斥状态：进入清除确认后隐藏恢复文件和密码表单，以一次明确的二次按钮确认替代口令输入。
- 创建钱包时一次下载加密备份与独立恢复材料；以轻量分隔行解释用途，不用大面积警告卡或额外下载步骤打断流程。
- 加密备份导入与私钥恢复必须是互斥状态：前者输入备份原密码，后者输入私钥、RootSeed，并设置和确认新的 keystore 密码。不得把两套控件同时留在页面上。
- 深浅主题是同一材质系统，不做机械颜色反转。
- 中文和英文都必须自然换行，不依赖固定字符宽度。
- 演示数据必须明确标注，不能暗示来自实时系统。

### 选择控件

- 少量互斥选项统一使用 `SegmentedControl`。控件只有一个共享选择镜片，镜片在选项之间平移；不得为每个选项分别堆叠实体背景或阴影。
- 选择镜片使用低饱和蓝灰材质，状态切换约 190ms、无弹跳；按压反馈约 120ms，并在减少动态效果时退化为即时颜色变化。
- 较长或动态选项统一使用 `AppSelect`。桌面端菜单从触发控件原点展开，移动端交给系统原生选择器处理。
- 下拉选项优先显示可读名称，地址、ID 等技术值作为第二行并安全截断；完整值仍由表单状态保存，不得因展示省略而改变。
- 分段控件和下拉控件必须支持键盘、清晰焦点、禁用状态、长文本与深浅主题。后续页面不得重新手写原生 `select` 外观或另一套切换器。

## 反模式

- 三张同尺寸功能卡作为首页主体。
- 无意义的发光边框、玻璃卡片和背景粒子瀑布。
- 伪造实时余额、法币价格、TPS 或延迟。
- 同一页面出现多个品牌强调色。
- 按钮只有图形但没有可访问名称。
